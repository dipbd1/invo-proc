# @invo/shared

Zod contracts, partner matching, and the same floor-per-tax-code math as the accounting mock.

```bash
cd packages/shared
npm install
npm test
```

Ingest and review depend on this package with `"@invo/shared": "file:../../packages/shared"`.
