# Accounting API (frozen mock)

Python 3.9+, standard library only. Copied verbatim from `static/take-home/TAKE_HOME.md` section 8. Do not change behaviour.

```bash
python3 accounting_api.py
curl http://localhost:8080/health
```

API key for every route except `/health`: `X-API-Key: demo-key-1234`
